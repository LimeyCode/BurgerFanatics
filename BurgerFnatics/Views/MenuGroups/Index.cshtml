@model IEnumerable<BurgerFnatics.Models.MenuGroup>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Menu)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MenuGroupName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MenuGroupDescription)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Menu.MenuID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MenuGroupName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MenuGroupDescription)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.MenuGroupID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.MenuGroupID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.MenuGroupID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
